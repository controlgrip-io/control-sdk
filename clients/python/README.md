# ControlGrip client — see the [root README](../../README.md) for the shared surface and quickstart.

The client includes GitHub App integration helpers for status, browser-assisted
connection, repository and branch discovery, file checks, disconnection, and
the `github_source(...)` task-source descriptor.
