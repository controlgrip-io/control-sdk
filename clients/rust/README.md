# ControlGrip client — see the [root README](../../README.md) for the shared surface and quickstart.

GitHub App methods are available as `github_status`, `connect_github`,
`disconnect_github`, `github_repositories`, `github_branches`, and
`github_repository_file_exists`. `connect_github` returns a URL that must be
opened in a browser to complete GitHub authorization and App installation.
